name: Generate Snake

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  generate:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Generate snake
        uses: Platane/snk@v3
        with:
          github_user_name: Chipzy98
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Deploy snake
        uses: crazy-max/ghaction-github-pages@v4
        with:
          build_dir: dist
          build_commit_message: "Update contribution snake"
        env:
          GH_PAT: ${{ secrets.GITHUB_TOKEN }}
